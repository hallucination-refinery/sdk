# 2025-07-08 Scratchpad

## Task schema-1 · Extract `@refinery/schema`

### Plan

- Create branch `feat/schema-extract`.
- Scaffold package structure.
- Move and rename types.
- Update imports via `ts-morph` or codemod.
- Green build & tests.

### TODO

- [ ] Branch created
- [ ] Package scaffolded
- [ ] Types copied & renamed
- [ ] Imports updated
- [ ] Build + type-check green
- [ ] Tests added & pass
- [ ] Coverage ≥ 80 %
- [ ] Demo build green (dev + prod)
- [ ] PR opened

### Evidence

**2025-07-08 14:30** - Initial observations:
- The `@refinery/schema` package already exists with core types defined
- IdeaNode, Edge, Graph types are already in schema with Zod validation
- Many packages already import from @refinery/schema (~41 files)
- Intent types currently live in `@refinery/input-hub` - need to move to schema
- Some legacy imports from local types files still exist in various packages
- Schema package already has proper exports and Zod setup

**2025-07-08 14:35** - Actions taken:
- Created Intent schema in packages/schema/src/core/intent.ts with Zod validation
- Renamed IdeaNode to Node throughout schema package (keeping legacy exports for compatibility)
- Renamed idea-node.ts to node.ts
- Updated test files to use new Node naming

**2025-07-08 14:40** - Import updates:
- Updated input-hub to import Intent types from @refinery/schema
- Fixed type conflicts with IntentValues const
- Type-check now passes successfully: `pnpm run type-check` ✓

---

## Prompt template for Claude Code

```text
Ultrathink, observe, orient, decide and execute: **Extract core schema package (task schema-1)** so that the workspace exposes `@refinery/schema` with Node/Edge/Intent types validated by Zod. Demo must compile & run unchanged.

1. Use `.taskmaster/scratchpads/2025-07-08.md` as your working scratchpad – update it after every observation (build log, test result, coverage number).
2. Make **atomic commits** referencing `(task schema-1)`; push to `feat/schema-extract`.
3. Treat every evidence point as Bayesian signal: if anything threatens W, enlarge Δ in the scratchpad and ask for guidance instead of guessing.
4. CI must stay green; run lint, type-check, unit-tests, build demo (dev & prod) locally before pushing.
5. Follow guidelines in `CLAUDE.md` (commit style, guard-rails, no force-push).
```
