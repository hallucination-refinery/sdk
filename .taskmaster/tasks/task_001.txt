# Task ID: 1
# Title: Bootstrap pnpm Workspace and Development Environment
# Status: pending
# Dependencies: None
# Priority: high
# Description: Set up the foundational workspace structure with pnpm workspaces, dev-container configuration, and initial tooling
# Details:
Create root package.json with pnpm workspace configuration. Set up dev-container with Node 20, pnpm 9, Oh-My-Zsh, and Claude CLI. Configure TypeScript 5, Vitest, and Turbo in workspace root. Add pnpm overrides for three@0.176.0, react@19.1.0, @react-three/fiber@^9.1.2. Create initial directory structure: packages/schema, packages/ops, packages/store, packages/canvas, packages/input, packages/widgets, apps/hello-sdk. Set up .env.example with OPENAI_API_KEY and ELEVEN_LABS_API_KEY stubs.

# Test Strategy:
Verify workspace setup by running 'pnpm -r run build' and 'pnpm -r exec vitest run' commands successfully. Confirm dev-container builds and all specified tools are available.
