Run `ensure-playwright` before any orchestrated workflow to guarantee a non-root browser cache is present.

Commands

```
/ensure-playwright
```

If slash commands are unavailable, invoke the script directly:

```
bash scripts/ensure-playwright.sh
```
