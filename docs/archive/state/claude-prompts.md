### Claude Code Prompts (Assembly Line)

1. **cv-build**

```
Ultrathink, observe, orient, decide, execute: align webpack/Three resolution and Tailwind config for cryptiq-mindmap; copy data bundles (excluding graph.automerge) — see apps/cryptiq-mindmap and apps/legacy-import/cryptic-vault-demo/config files.
```

2. **cv-integrate**

```
Ultrathink, observe, orient, decide, execute: wire IdeaCanvas + GraphForgeLoader to /api/mindmap nodes route and render 71-node graph — files in apps/cryptiq-mindmap/app.
```

3. **cv-optimize**

```
Ultrathink, observe, orient, decide, execute: scale to 1k nodes, port HUD components, add vitest ≥80% and push Vercel preview — focus on apps/cryptiq-mindmap/components & tests.
```
